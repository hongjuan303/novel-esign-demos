# 小说业务接入腾讯电子签 PRD（V1.1 决策回填稿）

## 1. 文档结论

本期把现有“主编确认购买—线下收资料—法务出合同—E签宝签署—人工跟进”的链路改造为：作者在申请签约前完成合同所需资料，绿台选择买断/保底模板并发起腾讯电子签，作者先签、平台后签，系统以腾讯电子签回调为准同步状态，并把已签合同和证据报告回写小说版权文件。

首版采用“模板发起 + 作者个人签署 + 企业方后签 + H5 跳转”的 B2C 方案。企业认证、应用、印章和平台签署方式尚未配置，属于生产上线阻塞项；Demo 中的发起与平台签署仅作交互模拟。签约业务状态与腾讯电子签流程状态分层保存，不直接把第三方状态展示给业务人员。

### 1.1 本轮已确认决策

| 事项 | 当前结论 | 产品处理 |
|---|---|---|
| 腾讯电子签企业准备 | 尚未完成 | 标记为上线阻塞；完成企业认证、应用、印章与签署人配置后才能生产发起 |
| 签署顺序 | 作者先签 | 作者签完后进入“待平台签署”，双方完成后合同才生效/归档 |
| 合同模板 | 买断、保底两种 | 按编辑审核结果强绑定模板，不允许任意错选 |
| 审批 | 待法务确认 | 首版用配置开关预留“待审批”；未确认前不写死审批节点 |
| 作者资料时点 | 申请签约之前完成 | 资料不完整时禁用“申请签约”，合同发起后冻结关键字段 |
| 服务端/回调资料 | 待提供 | 回调、对象存储、告警与负责人仍为 P0 技术缺口 |
| 版权文件上限 | 占用现有名额 | 已签合同与证据报告各占 1 个，发起前至少剩余 2 个名额 |
| 历史 E签宝 | 完成的手动上传；未完成的继续线下 | 不迁移、不切换中途流程；仅切换日后新合同走腾讯电子签 |

### 1.2 已核对的现网与原型基线

本稿已基于飞书说明、四个蓝湖原型和已登录绿台页面复核。现有逻辑不是从零建设，腾讯电子签应替换现有人工签约节点：

- 作者端已有“作品管理、个人中心、收益管理”，作品状态为“草稿、审核中、待签约、签约完成、终止签约”。
- 编辑在“作者投稿管理”审核作品时，已录入“签约方式（买断/保底＋分成）”和“签约稿费”。
- 现有“签约”弹窗由商务手工选择“签约完成/终止签约”，填写 E签宝合同编号并上传 PDF。
- 签约完成数据进入“小说稿费审核”，财务审核通过后通知云账户打款；作者端收益列表展示合同 ID、签约性质、税前收入、到账状态和签约日期。
- 现网“小说管理”已有小说 ID、作者、编辑、责编、部门、上架类型、版权类型、版权文件等字段，版权合同 PDF 直接展示在“版权文件”列。
- 小说数据权限为：经理可看本部门全部小说；主编可看下属责编及自己负责的小说；责编只看自己的小说。电子签合同及操作日志必须继承同一数据范围。

因此本期不新增独立“签约中心”或平行合同库页面；作者端在“作品管理”完成签署，绿台在现有小说/投稿签约入口发起和跟踪，已签 PDF 回写现有“版权文件”字段。

## 2. 背景与目标

- 当前稿件、作者资料、合同与打款分散在邮箱、IM、表格和电子签平台，状态不可追溯。
- 作者重复提交身份证、银行卡等敏感信息，存在体验与合规风险。
- 新目标：主编与作者的签约全线上流转留痕，合同状态可查询、异常可恢复、文件可审计。
- 本期不默认扩张至投稿获客、内容审核、议价在线化及自动付款；这些能力需单独立项。

## 3. 角色与权限

| 角色 | 核心权限 |
|---|---|
| 作者 | 核对签约信息、补充实名资料、查看合同、跳转签署、查看状态、下载已签合同 |
| 主编/编辑 | 选择作品和作者、录入签约参数、提交/发起合同、催签、撤回、查看留痕 |
| 商务/法务 | 维护合同模板及版本、复核条款、处理异常、下载合同与证据报告 |
| 财务 | 只读合同完成状态、校验收款主体、处理提现/付款 |
| 系统管理员 | 配置腾讯电子签应用、回调、操作人、印章、模板映射及权限 |

建议将“录入参数”“审批参数”“发起签约”“撤销合同”“下载敏感文件”拆成独立权限点。

## 4. 端到端主流程

1. 作者完善签约资料。至少覆盖实名、证件、手机号、联系地址、收款账户及合同要求的发表情况；未完整时不能点击“申请签约”。
2. 作者申请签约，作品进入“审核中”；编辑按现有流程审核。
3. 编辑选择“审核通过”并保存现有字段：小说名称、导语、完结时间、字数、签约方式、签约稿费，作品进入“待签约”。
4. 主编/有权限的业务人员在现有签约入口点击“发起腾讯电子签”；系统按签约方式自动匹配买断或保底模板，并复用审核金额。
5. 系统校验：作者合同资料、作品归属、签约方式、金额、模板版本、企业主体、签署控件均完整；版权文件至少剩余 2 个名额。
6. 若法务确认需要审批，签约单先进入“待审批”；若不需要则由有权限人员直接发起。该规则用配置开关控制。
7. 绿台调用腾讯电子签创建流程 `CreateFlow`，保存 `FlowId`、请求 ID 与幂等键；再调用 `CreateDocument` 填充模板控件，等待文档合成。
8. 调用 `StartFlow` 启动签署，第一顺位固定为作者。
9. 作者端出现“查看并签署合同”。点击时通过 `CreateFlowSignUrl` 实时生成 H5 链接。
10. 作者在腾讯电子签中完成主合同签署页和附件《版权转让声明函》两处签名。返回投稿后台后，最终结果仍以服务端回调为准。
11. 作者签完后进入“待平台签署”；平台指定经办人/印章按最终配置完成第二顺位签署。
12. 双方签署完成后自动获得合同编号和签约日期，下载已签合同 PDF 与证据报告，二者各占 1 个现有版权文件名额并回写小说管理。
13. 归档成功后作品转为“签约完成”，并沿用现有“小说稿费审核—财务审核—云账户打款”链路。

## 5. 状态机

对作者保持现有五态，不暴露第三方细分状态：

`草稿 → 审核中 → 待签约 → 签约完成`

`审核拒绝 / 签约拒绝 / 合同过期且不再续签 → 终止签约`

绿台在“待签约”内增加电子签子状态：

`待发起 →（可选：待审批）→ 生成中 → 待作者签署 → 待平台签署 → 归档中 → 签署完成`

展示映射：

| 腾讯/绿台子状态 | 作者端作品状态与文案 |
|---|---|
| 待发起、生成中 | 待签约：电子合同准备中 |
| 待作者签署 | 待签约：显示“查看并签署合同” |
| 待平台签署 | 待签约：作者已签，等待平台方签署 |
| 归档中 | 待签约：签署结果同步中 |
| 签署完成且文件归档成功 | 签约完成 |

异常终态或分支：

- `生成失败`：允许重试，不重复创建成功的腾讯流程。
- `签署拒绝`：记录拒签人、时间、原因；回到“待处理”，不可直接重用旧合同。
- `已撤销`：仅有权限人员可撤销，必须填写原因。
- `已过期`：允许基于原参数复制新签约单，旧单保留。
- `归档失败`：签署仍为完成，但不能提现；归档重试成功后解锁。
- `信息变更待重签`：作者姓名、证件、金额、主体或核心条款变更时，撤销旧流程并创建新版本。
- `文件名额不足`：发起前拦截；业务先清理现有版权文件或调整归档方案，不能等双方签完后再发现无法回写。

每次状态变化保存：签约单 ID、前后状态、业务动作、操作人/系统、时间、原因、腾讯 `FlowId`、`RequestId`、回调事件 ID、结果摘要。

## 6. 作者投稿后台

### 6.1 作品管理迭代

- 不新增顶级导航；在现有“作品管理”卡片中增强“待签约”和“签约完成”状态。
- `待发起/生成中`：保留“待签约”，提示“编辑正在准备电子合同”，不展示无效签署按钮。
- `待作者签署`：展示“查看并签署合同”、签署截止时间和合同摘要。
- `待平台签署`：展示“作者已签，等待平台方签署”，作者不可重复签署。
- `签署完成`：沿用“查看收益、小说详情”，新增“查看合同/下载已签合同”。
- 签署前展示作品名、签约方式、税前稿费、签约双方、脱敏姓名/手机号、截止时间。
- “前往腾讯电子签”仅在作者是当前签署方且流程可签时出现。

### 6.2 签署交互

- 点击后请求服务端生成新 H5 链接，再跳转腾讯电子签；签署页面包含主合同和声明函两处作者签名控件。
- 返回页轮询本系统状态或等待推送；超时提示“签署结果同步中”并提供刷新。
- 拒签、过期、撤回、链接失效、人脸核验失败均给出明确下一步。
- 作者不得在投稿后台直接编辑已写入合同的关键参数。

### 6.3 个人中心与收益管理

- 个人中心继续维护作者与收款资料；资料完整是“申请签约”的前置条件。合同已发起后，姓名、证件号、手机号、地址、收款账户等关键字段冻结。
- 原型中“作者有一次签约记录后全部账号和签约信息不可修改”过于严格，应拆为：签署身份字段需走主编审核，微信/QQ/邮箱/地址可正常更新，银行卡变更单独审核且不影响已签合同。
- 收益管理保持现有字段；合同 ID 改为腾讯电子签合同编号，点击查看系统归档的已签 PDF。
- 税前收入仍取编辑审核时录入的签约稿费；到账状态仍取云账户，不由腾讯电子签决定。

## 7. 内部绿台

不新增独立“签约管理”菜单。首版将电子签能力嵌入现有两个位置：

1. “作者投稿管理”的待签约操作：原“签约（手工上传合同）”替换为“发起签约/查看签约”。
2. 现网“小说管理”：新增筛选项与列表列“签约状态”，已签合同回写既有“版权文件”，操作列新增“发起签约/查看签约”。

### 7.1 列表

在现网筛选项基础上只增加“签约状态”。签约详情中再提供合同编号、模板和异常筛选，避免进一步加宽已很长的小说列表。

操作按状态显示：

- 待发起：发起签约。
- 生成中：查看进度、撤销。
- 待作者签署：查看签约、催签、撤销。
- 待平台签署：查看签约、通知/进入企业方签署、撤销（是否允许撤销待法务确认）。
- 签署完成：查看合同、下载合同、查看留痕。
- 异常：重试/复制重签，原流程只读保留。

### 7.2 新建/发起

必填字段：

- 作品与作者；签约主体；合同名称；合同模板及版本。
- 签约金额、币种、含税口径、付款条件。
- 权利类型、地区、渠道、期限、是否独家、是否可转授权。
- 作者签署姓名、手机号、证件类型/号码来源。
- 企业经办人、企业签署方式、签署顺序（固定作者先签）、签署截止时间。

发起前展示最终参数快照。提交后生成不可变版本；改关键字段必须生成新版本并重新签署。

字段来源：

| 电子合同字段 | 来源 |
|---|---|
| 小说名称、作者笔名、责编、部门 | 小说管理 |
| 签约方式、税前稿费 | 编辑审核结果 |
| 作者实名、手机号、证件号 | 作者管理/个人中心 |
| 合同模板、签约主体、企业签署方式 | 电子签系统配置；当前尚未完成 |
| 合同编号、签约日期、已签 PDF | 腾讯电子签回调/查询 |

### 7.3 详情与留痕

- 展示业务状态和腾讯流程状态。
- 展示模板字段映射结果、签署方顺序、发起人、操作人、回调时间轴。
- 文件区：原模板版本、待签 PDF、已签 PDF、证据报告、撤销/拒签记录。
- 异常区：错误码、请求 ID、最近重试、可执行动作。
- 数据权限沿用小说权限；合同下载、撤销、重签再叠加功能权限。服务端必须校验，不能只在前端隐藏。

### 7.4 两类合同模板与字段映射

已读取本次提供的两份 Word 模板。两份均为《版权转让合同》，均含附件《版权转让声明函》，但商业条款不同，不能只维护一个通用金额字段。

| 字段组 | 买断模板 | 保底模板 | 建议来源 |
|---|---|---|---|
| 企业信息 | 名称、联系人、地址、电话、统一社会信用代码 | 相同 | 企业配置 |
| 作者信息 | 实名/机构名、联系人、地址、手机号、身份证号/统一社会信用代码、笔名 | 相同 | 作者签约资料 |
| 作品信息 | 名称、字数、简介、完成日期、是否首发、首发日期/平台 | 相同 | 小说信息＋作者确认 |
| 商业字段 | 固定版权费用（数字及大写） | 保底费用（数字及大写）、分账周期、结算频率 | 编辑审核结果＋业务填写 |
| 分成规则 | 无 | 信息流净利润作者 70%；外部授权/分发净利润作者 50%；自制演绎净利润作者 10%；第三方演绎授权收入作者 50%；低于 100 元顺延 | 当前模板固定值，法务确认后锁定在版本中 |
| 收款/税务 | 开户行、户名、账号、个人或机构主体；机构发票信息 | 相同 | 作者签约资料＋企业开票配置 |
| 签名控件 | 企业盖章 1 处；作者主合同签名 1 处；声明函签名 1 处 | 相同 | 腾讯模板控件映射 |
| 签署日期 | 双方完成签署日期；声明函日期 | 相同 | 电子签平台自动写入 |

模板控制规则：

- 编辑选择“买断”时只能使用买断模板；选择“保底＋分成”时只能使用保底模板。
- 数字金额自动转换大写金额，二者必须一致；保底模板分账周期和结算频率不得留空。
- 模板、分成比例、税务条款或权利条款变化必须升版本；已发起流程继续引用旧版本快照。
- 文件名中的“杭州宝茂”不能替代合同主体配置，因为 Word 正文中的甲方全称、联系人、地址、电话和统一社会信用代码目前均为空。
- 两份模板写明合同自双方完成有效电子签名后生效，与“作者先签、平台后签”的状态设计一致。
- 是否必须保留声明函的独立作者签名，以及企业盖章是否只落在主合同签署页，仍需法务确认。

## 8. 腾讯电子签集成设计

- 使用自建应用集成，联调环境与线上环境完全隔离；联调合同无正式法律效力。
- 凭证仅存服务端密钥管理系统，前端不能持有 SecretId/SecretKey。
- 使用腾讯云 SDK；所有写接口加业务幂等键、超时重试和请求日志。
- 模板流程：`CreateFlow → CreateDocument → 文档合成回调 → StartFlow → CreateFlowSignUrl`。
- H5 链接默认短时有效，按需生成；签署完成通过 HTTPS `JumpUrl` 回到作者后台。
- 回调必须校验来源/签名、防重放、按事件 ID 幂等消费；乱序回调不能导致状态倒退。
- 生产发布前必须完成企业认证、应用配置、操作人 UserId、模板 ID、企业印章、平台签署人/授权方式、回调地址及 IP/域名配置。当前均未完成，不具备生产发起条件。
- 参与方顺序固定为作者第 1 顺位、企业第 2 顺位；不能再使用“企业自动先签”的配置。
- 买断、保底分别维护腾讯模板 ID 和字段/签名控件映射，映射发布前做一次完整预览与法务验收。

## 9. 数据模型

- `contract_order`：业务签约单、作品、作者、金额、条款快照、版本、业务状态。
- `contract_party`：企业/个人签署方、顺序、认证方式、脱敏展示值及第三方参与方 ID。
- `esign_flow`：FlowId、模板 ID、腾讯状态、发起/完成/过期时间、环境。
- `esign_event`：事件 ID、类型、验签结果、原始报文密文、处理状态、重试次数。
- `contract_file`：文件类型、哈希、对象存储地址、版本、下载权限、归档时间。
- `contract_audit_log`：动作、操作人、前后值、IP/设备（按合规策略）、RequestId、原因。

身份证、银行卡等敏感字段应加密存储、脱敏展示、按角色授权访问，并设置访问审计与保留/删除策略。

## 10. 异常与兜底

- 创建流程超时：先按幂等键查询，不盲目重发。
- 文档合成回调超时：定时查询状态并告警。
- 作者签署链接过期：实时重新生成，不新建合同。
- 回调丢失：定时任务以 `FlowId` 对账并补偿。
- 签署后归档失败：锁定提现，自动重试并通知管理员。
- 作者拒签/合同撤销：冻结旧版本，保留全部证据，复制参数发起新版本。
- 腾讯服务不可用：前端提示稍后重试，绿台显示第三方故障，不把状态标成业务失败。

### 10.1 历史合同与切换策略

- 以双方约定的“腾讯电子签切换时间”为边界；切换后新发起合同才进入腾讯流程。
- 切换日前已经完成的旧 E签宝/线下合同，由业务继续在小说管理手动上传 PDF，标记来源“历史线下”。
- 切换日前尚未完成的合同保持原线下流程，签完后仍由业务手动上传；不迁移、不补建腾讯流程 ID。
- 历史线下合同不生成腾讯证据报告，不伪造线上流转日志；只记录上传人、上传时间和原合同来源。
- 历史合同与腾讯合同共用现有版权文件上限。腾讯合同固定占 2 个名额；历史线下合同按实际上传文件数占用。

## 11. 验收标准

- 主编能从绿台对合格作品发起合同，重复点击不会生成重复合同。
- 作者资料未完整时不能申请签约；完整后能从投稿后台进入腾讯电子签并完成主合同与声明函签署。
- 作者完成后状态进入“待平台签署”，平台完成后才进入“归档中/签署完成”。
- 回调重复、乱序、延迟时，业务状态不倒退且最终一致。
- 签署完成后合同文件、证据文件、FlowId、模板版本、完整操作日志均可查询。
- 发起时剩余版权文件少于 2 个必须拦截；归档后已签合同和证据报告分别占 1 个名额。
- 已完成与未完成的历史 E签宝合同均按既定线下策略处理，不被误迁移到腾讯流程。
- 未完成签署或归档失败时不能提现；归档成功后自动解锁。
- 无权限人员不能查看完整身份证、下载合同、撤销或重新发起。

## 12. 当前逻辑缺失与需业务补充

### P0：决定流程能否落地

1. **企业主体与腾讯配置**：提供甲方工商全称、统一社会信用代码、地址、联系人/电话；完成腾讯电子签企业认证、应用、印章、模板和平台签署人配置。文件名中的“杭州宝茂”不足以生成正式合同。
2. **平台如何后签**：作者先签已确认，但企业方是法人/主编/指定经办人手签、企业静默签署还是授权印章自动签仍未确认；需要签署人姓名、手机号、腾讯 UserId 及用印授权方式。
3. **法务结论**：是否需要发起前审批、金额分级、待平台签署时能否撤销；声明函是否必须独立签名；企业章落点；两份模板正文、分成比例、税务和永久转让条款是否可直接上线。
4. **保底模板业务值**：分账周期是几年、按月/季度/年度结算；当前 30%/70%、50%/50%、10%、50% 和 100 元顺延阈值是否固定，是否存在作品级例外。
5. **作者类型**：首版只支持中国大陆个人作者，还是同时支持港澳台/其他证件、未成年人、工作室和公司作者；不同类型决定实名、监护人、发票与签署方结构。
6. **技术接入资料**：服务端技术栈、腾讯 Secret 管理方式、生产/测试环境、回调公网域名、对象存储、消息队列/定时对账、告警渠道、接口负责人。
7. **归档可行性**：腾讯返回的已签合同和证据报告是否都满足现有单文件 10M；若任一文件超过 10M，需要压缩、独立存储或调整上限。

### P1：决定产品体验

8. **作者资料表单**：联系地址、首次发表状态/日期/平台、开户行/户名/账号、个人/机构纳税类型分别从哪里维护，由谁审核；申请签约后哪些字段冻结。
9. **权限矩阵**：谁能发起、催签、查看未脱敏证件、下载合同/证据报告、撤销、重签、手动上传历史合同；不同金额是否需要额外角色。
10. **通知规则**：作者签署和平台签署分别使用站内、短信、微信或腾讯通知；首次、到期前、逾期后催签频率和费用承担。
11. **异常责任人**：拒签、过期、撤销、回调失败、文件名额不足、合同更正、解约分别进入谁的待办，处理时限与是否二次审批。
12. **收益衔接**：买断/保底费用何时进入稿费审核；保底模板的后续分成结算是否仍在现有收益系统，本期是否只处理首笔保底费用。
13. **切换时间**：历史处理方式已确认，仍需给出精确切换时间和判断口径（按合同创建时间、审核通过时间或实际发起时间）。
14. **页面入口确认**：当前生产“小说管理”中未看到蓝湖旧稿的“作者投稿管理、小说稿费审核”，需确认真实菜单、权限或替代页面，以冻结最终入口。

### 资料缺口

- 已完成四个蓝湖链接、作者投稿后台说明、绿台小说管理和两份正式 Word 模板的只读核对。
- 请提供：企业工商/签署资料、法务对上述模板与审批的确认、保底周期/结算频率、作者类型范围、腾讯电子签配置截图/参数、服务端与回调资料。
- 请指定业务、法务、后端、测试、财务的验收负责人。

## 13. 版本建议

- V1.0：仅覆盖切换日后新合同；买断/保底模板发起、作者先签、平台后签、回调同步、双文件回写、查看进度、催签/撤销、稿费审核数据自动衔接。
- V1.1：按法务结论上线合同参数审批、批量发起/催签、异常工作台、对账报表。
- V1.2：解约/补充协议、模板在线版本治理、自动付款/税务协同。
